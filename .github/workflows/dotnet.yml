name: .NET

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: windows-latest   # ✅ Windows runner kullanıyoruz

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --configuration Release --no-restore

    - name: Publish
      run: dotnet publish -c Release -o publish

    - name: Zip artifact
      run: Compress-Archive -Path publish -DestinationPath app_build.zip -Force

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: app_build
        path: app_build.zip
